import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChemdatabaseSpillPage } from './chemdatabase-spill.page';

describe('ChemdatabaseSpillPage', () => {
  let component: ChemdatabaseSpillPage;
  let fixture: ComponentFixture<ChemdatabaseSpillPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChemdatabaseSpillPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
