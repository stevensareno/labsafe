import { ComponentFixture, TestBed } from '@angular/core/testing';
import { ChemdatabaseSkincontactPage } from './chemdatabase-skincontact.page';

describe('ChemdatabaseSkincontactPage', () => {
  let component: ChemdatabaseSkincontactPage;
  let fixture: ComponentFixture<ChemdatabaseSkincontactPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(ChemdatabaseSkincontactPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
